const { EmbedBuilder } = require("discord.js");
function execute(interaction) {
    
    interaction.reply("Shared Discret math unit 4.1 notes with english")

}
module.exports = {
    execute,
    name: "share"

}

